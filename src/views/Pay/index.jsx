import React from 'react';
import { NavLink } from 'react-router-dom';

export const Pay = () => {
    return (
        <div>
            <NavLink to="/home">
                  Click to Home
            </NavLink>
        </div>
    );
};
